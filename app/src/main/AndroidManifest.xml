<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AdvisorApps"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegistrasiActivity" />
        <activity android:name=".MainKendaraan" />
        <activity android:name=".MainActivity" />
        <activity android:name=".MainActivityForeman" />
        <activity android:name=".MainActivityProfileUser" />
        <activity android:name=".MainService" />
        <activity android:name=".MainInfoService" />
        <activity android:name=".MainPemberitahuan" />
        <activity android:name=".MainLaporan" />
        <activity android:name=".MainPayment" />
        <activity android:name=".MainGanti" />
        <activity android:name=".MainSaranForeman" />
        <activity android:name=".MainServiceForeman" />
        <activity android:name=".MainItemForeman" />
        <activity android:name=".MainUpItemForeman"/>
    </application>

</manifest>